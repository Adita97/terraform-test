{
  "name": "auth0-infrastructure",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "NODE_PATH=src jest --runInBand"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "esbuild": "^0.25.1",
    "glob": "^13.0.0"
  }
}